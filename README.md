# -_-_calculadora_de_escritorio_en_c-_winforms-oracle19c_- :.
# 🧮 Calculadora de Escritorio en C# (WinForms + Oracle 19c):

<img width="1024" height="1024" alt="image" src="https://github.com/user-attachments/assets/4256d722-7a77-4fc7-a940-9fa7a3305ba0" />      

<img width="2552" height="1079" alt="image" src="https://github.com/user-attachments/assets/2c95de3d-899d-4e15-8e54-996c86b38ae1" />    

A continuación tienes una **solución profesional y funcional** para **Visual Studio 2022**:  
una **calculadora de escritorio en C# (WinForms)** que **registra los cálculos en Oracle 19c** mediante un **procedimiento almacenado** .

---

## 🎯 Enfoque Técnico

- **Plataforma**: .NET (WinForms)
- **IDE**: Visual Studio 2022
- **Base de Datos**: Oracle 19c
- **Acceso a Datos**: Oracle Managed Data Access
- **Arquitectura**: Separación por capas (Model / Data / UI)
- **Persistencia**: Stored Procedure

---

## 🧱 Arquitectura del Proyecto:

```text
CalculadoraOracle
│
├── Model
│   └── Operacion.cs
│
├── Data
│   └── OperacionDAO.cs
│
├── UI
│   └── FrmCalculadora.cs
│
└── Program.cs

🗄️ Base de Datos – Oracle 19c:
📌 Tabla
CREATE TABLE OPERACIONES_CALC (
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    OPERANDO1 NUMBER,
    OPERANDO2 NUMBER,
    OPERACION VARCHAR2(10),
    RESULTADO NUMBER,
    FECHA TIMESTAMP DEFAULT SYSTIMESTAMP
);
📌 Procedimiento Almacenado (Insert)
CREATE OR REPLACE PROCEDURE SP_GUARDAR_OPERACION (
    P_OP1       IN NUMBER,
    P_OP2       IN NUMBER,
    P_OPERACION IN VARCHAR2,
    P_RESULTADO IN NUMBER
) AS
BEGIN
    INSERT INTO OPERACIONES_CALC
    (OPERANDO1, OPERANDO2, OPERACION, RESULTADO)
    VALUES
    (P_OP1, P_OP2, P_OPERACION, P_RESULTADO);
END;
/

📦 Dependencias (NuGet):
En Visual Studio 2022, instalar:
Oracle.ManagedDataAccess .

📁 Capa Modelo:
Model/Operacion.cs
namespace Model
{
    public class Operacion
    {
        public double Operando1 { get; set; }
        public double Operando2 { get; set; }
        public string Tipo { get; set; }
        public double Resultado { get; set; }
    }
}

📁 Acceso a Datos (DAO):
Data/OperacionDAO.cs
using Oracle.ManagedDataAccess.Client;
using Model;

namespace Data
{
    public class OperacionDAO
    {
        private readonly string cadena =
            "User Id=USUARIO;Password=CLAVE;Data Source=localhost:1521/ORCLPDB1";

        public void GuardarOperacion(Operacion op)
        {
            using (OracleConnection cn = new OracleConnection(cadena))
            {
                cn.Open();

                OracleCommand cmd = new OracleCommand(
                    "SP_GUARDAR_OPERACION", cn);

                cmd.CommandType = System.Data.CommandType.StoredProcedure;

                cmd.Parameters.Add("P_OP1", op.Operando1);
                cmd.Parameters.Add("P_OP2", op.Operando2);
                cmd.Parameters.Add("P_OPERACION", op.Tipo);
                cmd.Parameters.Add("P_RESULTADO", op.Resultado);

                cmd.ExecuteNonQuery();
            }
        }
    }
}

🖥️ Interfaz Gráfica (WinForms):
Diseño del Formulario FrmCalculadora
Controles:
txtA → Operando 1
txtB → Operando 2
lblResultado → Resultado

Botones:
btnSuma (+)
btnResta (-)
btnMult (*)
btnDiv (/)

🎯 Lógica de la Calculadora:
UI/FrmCalculadora.cs
using Model;
using Data;

namespace UI
{
    public partial class FrmCalculadora : Form
    {
        private OperacionDAO dao = new OperacionDAO();

        public FrmCalculadora()
        {
            InitializeComponent();
        }

        private void Calcular(string tipo)
        {
            double a = double.Parse(txtA.Text);
            double b = double.Parse(txtB.Text);
            double r = 0;

            switch (tipo)
            {
                case "+":
                    r = a + b;
                    break;

                case "-":
                    r = a - b;
                    break;

                case "*":
                    r = a * b;
                    break;

                case "/":
                    if (b == 0)
                        throw new Exception("División por cero");
                    r = a / b;
                    break;
            }

            lblResultado.Text = r.ToString();

            Operacion op = new Operacion
            {
                Operando1 = a,
                Operando2 = b,
                Tipo = tipo,
                Resultado = r
            };

            dao.GuardarOperacion(op);
        }

        private void btnSuma_Click(object sender, EventArgs e)
            => Calcular("+");

        private void btnResta_Click(object sender, EventArgs e)
            => Calcular("-");

        private void btnMult_Click(object sender, EventArgs e)
            => Calcular("*");

        private void btnDiv_Click(object sender, EventArgs e)
            => Calcular("/");
    }
}

✅ Resultado Final:
✔ Calculadora de escritorio en WinForms
✔ Interfaz gráfica clara y funcional
✔ Operaciones matemáticas básicas
✔ Persistencia en Oracle 19c
✔ Uso de Stored Procedure
✔ Arquitectura limpia y separada por capas / .
